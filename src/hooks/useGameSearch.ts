// This file is no longer needed as its functionality has been moved to useSearch.ts

// Import and re-export the hooks from useSearch.ts if you still need them
export {
    useUnitsSearch,
    useBuildingsSearch,
    useMissionsSearch,
} from "./useSearch";
